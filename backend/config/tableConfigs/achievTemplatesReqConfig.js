const achievTemplatesReqConfig = [
  "name",
  "type", // It can be one of the following: "daily", "weekly", "oneTime",
  "reward", // GRP (Game Ranking Points)
];

module.exports = achievTemplatesReqConfig;
